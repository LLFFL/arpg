extends Area2D
@onready var bat: CharacterBody2D = $".."
#var playerLayer = layer 4
#Set validation so that when player in array change state to go for player.
#adds the enemy to the targeting array
#func _on_body_entered(body: Node2D) -> void:
#	bat.targets.append(body)
#	pass 

#func _on_body_exited(body: Area2D) -> void:
#	bat.targets.erase(body)
#	pass
